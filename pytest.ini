[pytest]
# Only collect tests from tests/ — this repo also has manual/standalone
# scripts at the root (e.g. test_classifier_manual.py) that happen to
# match the test_*.py naming pattern but are meant to be run directly,
# not picked up by an automated `pytest` run.
testpaths = tests
